Programming Assignment 2 for EGCP-4310, Computer Networks

This assignent's goal was to fill in the code so that we could send an email to ourselves via the python program. We had to connect to Gmail's SMTP server and use its protocol to send an email just with SMTP protocols.

The password field is obviously not "REDACTED", it is an app password from Google.